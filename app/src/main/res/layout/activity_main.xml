<?xml version="1.0" encoding="utf-8"?>
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawer_main"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    tools:context=".ui.activity.MainActivity">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="?attr/colorPrimaryContainer"
            app:navigationIcon="@drawable/ic_expand_compact_nav_view"
            app:subtitleTextColor="?attr/colorOnPrimaryContainer"
            app:titleTextColor="?attr/colorOnPrimaryContainer" />

        <FrameLayout
            android:id="@+id/frame_main_fragment_container"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1" />
    </LinearLayout>


    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigation_view_main"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:background="?attr/colorPrimaryContainer"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_view_main_header"
        app:itemIconTint="?attr/colorOnPrimaryContainer"
        app:itemShapeFillColor="@color/color_nav_main_item"
        app:itemTextColor="?attr/colorOnPrimaryContainer"
        app:menu="@menu/menu_main_navigation_layout" />

</androidx.drawerlayout.widget.DrawerLayout>